import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.udemycourse.ionicangular',
  appName: 'ionic-first-proyect',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
